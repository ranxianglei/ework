{
  "name": "ework-router",
  "version": "0.1.12",
  "description": "Standalone webhook router for ework — receives events, routes to daemons by strategy, proxies replies",
  "type": "module",
  "bin": {
    "ework-router": "./bin/ework-router.js"
  },
  "files": [
    "src",
    "bin",
    "README.md"
  ],
  "scripts": {
    "start": "bun run bin/ework-router.js",
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "mysql2": "^3.11.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "typescript": "^5.6.0"
  }
}
